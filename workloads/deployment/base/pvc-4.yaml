---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: busybox-pvc-4
spec:
  accessModes:
    - ReadWriteOnce
  # Customize in your overlay.
  storageClassName: STORAGE_CLASS_NAME
  resources:
    requests:
      storage: 1Gi
