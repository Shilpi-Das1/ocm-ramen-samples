---
resources:
  - ../base
patches:
  # Customize for internal cephfs storage.
  - target:
      kind: PersistentVolumeClaim
      name: busybox-pvc
    patch: |-
      - op: replace
        path: /spec/storageClassName
        value: custom-cephfs-sc
      - op: replace
        path: /spec/accessModes
        value:
          - ReadWriteMany
